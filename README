# Secure Drop

To set up the environment for the first time, run in terminal:

```
export GOPATH=$(pwd)
```

```
go get github.com/google/uuid
go get github.com/nweaver/cs161-p2/userlib
```

```
cd src/proj2
```

```
go test
```

For any other time, run the same commands minus the "go gets".
